# M1 sub-project 3 — Polish (glossary, stats, icon)

> Scope committed 2026-07-22: full Korean glossary, stats screen, app icon, study-tool
> visual tone. The visual tone is already established (Toss-inspired tokens in
> `Theme.swift`, decision: chips understated, equity/EV forward) — steps 2–3 simply
> follow it, no separate tone workstream. Engine and drills packages are untouched, so
> the release test gate is not needed; `swift test --package-path GlassTableDrills`
> plus a build + screenshots is the gate per step.

- [x] **Step 1: Glossary doc + Korean copy pass.** Write `docs/glossary.md` — the
  canonical term table decision F (`decisions.md`) promised ("full glossary is built
  during UI-copy work"): Hangul actions/streets, Latin acronyms, bilingual concept
  terms. Audit every UI string in `GlassTable/Sources` against it; fix: home drill
  rows get the bilingual English caption (팟 오즈 · Pot Odds …) per decision F.
  Commit.

- [x] **Step 2: Stats screen.** `StatsView`: per-drill rows (streak, accuracy,
  answered) + an overall header, read from the existing per-drill `ProgressStore`
  files; entry via a chart toolbar button on Home. Same design system (white cards on
  `GT.surface`, Pretendard). Build, screenshot, commit.

- [x] **Step 3: App icon.** Deterministic script (`tools/make-appicon.swift`, CoreGraphics)
  renders a flat 1024×1024 icon — GT green field, white card + percent glyph (study
  tool, not casino). Output into `GlassTable/Resources/Assets.xcassets/AppIcon.appiconset`;
  add `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` to `project.yml`. Build, verify
  icon on simulator home screen, commit.

- [x] **Step 4: Final gate.** Drills tests pass; `git diff main --stat -- GlassTableEngine`
  empty; full-app launch screenshot set (home, stats, one drill). Update
  `docs/milestone-1.md` rough-shape checkboxes / memory. Commit.

> Steps 5–6 added 2026-07-23 after design review
> (`docs/specs/2026-07-23-m1-polish-design.md`): the approved design calls for an
> in-app glossary screen and a card-free icon.

- [ ] **Step 5: 용어집 screen.** `GlossaryView`: static ~10-entry list (Korean term +
  English pair + one-line Korean definition, sourced from `docs/glossary.md`), layout
  mirroring `StatsView` (green header + white sheet). Entry: book toolbar button on
  Home next to the stats button; `GT_DEMO_GLOSSARY` debug hook mirroring
  `GT_DEMO_STATS`. Build, screenshot, commit.

- [ ] **Step 6: Card-free icon.** Rework `tools/make-appicon.swift`: drop the tilted
  card; render a subtle lighter "table" disc + large white % glyph on GT green.
  Re-render `AppIcon1024.png`, build, verify on simulator home screen, commit. Then
  final gate (drills tests, engine diff empty) and push.
